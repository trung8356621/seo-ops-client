> Status: Historical
> Not canonical
> Archived on: 2026-08-01
> Superseded by: ../../modules/AGENT_WORKSPACE.md
> Purpose: implementation history only
# Agent Cost & Usage

`AgentCostUsageTracker` reads provider usage metadata when present. Pricing from versioned registry only â€” if missing, `cost_unknown=true` (never fabricate). Dashboards site-scoped.
