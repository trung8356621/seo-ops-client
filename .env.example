APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://127.0.0.1:8010
SERVER_HOST=127.0.0.1
SERVER_PORT=8010

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
# DB_CORE_CONNECTION=mysql
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

# Automation runtime = core mysql (physical DB omi_client). Never default to omi_seo_ai.
AUTOMATION_DB_CONNECTION=mysql
# LEGACY upgrade-only copy path (automation:migrate-to-core). Unused after cutover.
# AUTOMATION_SOURCE_DB_CONNECTION=omi_seo_ai
# AUTOMATION_TARGET_DB_CONNECTION=mysql

# Addon DB: addon.json + database.local.php (addon khác). SEO Content AI: bảng core seo_database_connections.

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database
# Must be > RunContentProjectArticleJob::$timeout (900). Production target: 1200.
DB_QUEUE_RETRY_AFTER=1200

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# WP Headless: override URL Next.js cho webhook (receive/updated). Dùng khi "Failed to connect to localhost port 3000".
# VD: http://127.0.0.1:3000 (IPv4) hoặc http://host.docker.internal:3000 (Laravel trong Docker).
# WP_HEADLESS_NEXTJS_WEBHOOK_URL=http://127.0.0.1:3000

# Intervention Image v4: imagick | gd (để trống = tự chọn imagick nếu có, không thì gd)
# IMAGE_DRIVER=imagick

# Article List diagnostics (timing + slow SQL ≥300ms). Default off.
# SEO_ARTICLE_LIST_DIAGNOSTICS=false
# SEO_ARTICLE_LIST_SLOW_SQL_MS=300
# SEO_ARTICLE_LIST_QUEUE_BADGE_CACHE_SECONDS=15

# Debug «Tạo gợi ý liên kết» — log [LINK_FALLBACK_DEBUG] + suggestion_debug trong JSON.
# LINK_SUGGESTION_DEBUG=true

# Passive control client. Never poll ops-server. Enrollment is explicit Connect only.
# CLIENT_VERSION=0.0.9
# CLIENT_CONTROL_COMMAND_MAX_AGE=300
# CLIENT_CONTROL_FUTURE_SKEW=30
